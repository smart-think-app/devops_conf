def gitUrl = "https://github.com/smart-think-app/golang-service"
pipeline {
  agent any
  stages {
    stage("git pull code") {
      steps {
        dir('golang-service'){
          echo "git pull code ${gitUrl}"
          git url: "${gitUrl}", branch: "master"
        }
      }
    }
    stage("run unit test") {
      steps {
        echo "run unit test"
        sh "cd golang-service && ls"
      }
    }
    stage("build docker") {
      steps{
        echo "build docker"
        sh "docker build -t go-dk1 ."
      }
    }
    stage("deploy") {
      steps{
        echo "deploy"
      }
    }
  }
}